# i-MSCP Setup packages

Packages located in that directory are only processed in installer/uninstaller
contexts. If a package need to operate in another context, it must not be
stored in that directory.
