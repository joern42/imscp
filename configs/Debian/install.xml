<?xml version="1.0" encoding="UTF-8"?>
<install umask="0022">
    <folder mode="1777">/tmp</folder>
    <folder umask="0027" export="INST_PREF" pre_remove="1">/tmp/imscp</folder>
    <folder export="ROOT_CONF">${INST_PREF}/etc</folder>
    <folder umask="0027" export="SYSTEM_CONF">${INST_PREF}/etc/imscp</folder>
    <folder>${INST_PREF}/usr</folder>
    <folder>${INST_PREF}/usr/local</folder>
    <folder export="LOCAL_CONF">${INST_PREF}/usr/local/etc</folder>
    <folder export="LOCAL_USR_LIB">${INST_PREF}/usr/local/lib</folder>
    <folder export="LOCAL_USR_SBIN">${INST_PREF}/usr/local/sbin</folder>
    <folder>${INST_PREF}/var</folder>
    <folder>${INST_PREF}/var/log</folder>
    <folder umask="0027" export="SYSTEM_LOG">${INST_PREF}/var/log/imscp</folder>
    <folder>${INST_PREF}/var/mail</folder>
    <folder umask="0027" export="SYSTEM_MAIL_VIRTUAL">${INST_PREF}/var/mail/virtual</folder>
    <folder>${INST_PREF}/var/www</folder>
    <folder umask="0027" export="SYSTEM_ROOT">${INST_PREF}/var/www/imscp</folder>
    <folder export="SYSTEM_VIRTUAL">${INST_PREF}/var/www/virtual</folder>
</install>
