<?xml version="1.0" encoding="UTF-8"?>
<install>
    <folder umask="0022" create_if="index('${iMSCP::Servers::Ftpd}', '::Proftpd::') != -1">${ROOT_CONF}/logrotate.d</folder>
    <folder>${SYSTEM_CONF}/proftpd</folder>
    <copy_config>${SYSTEM_CONF}/proftpd/proftpd.conf</copy_config>
    <copy_config>${SYSTEM_CONF}/proftpd/proftpd.data.dist</copy_config>
    <copy_config umask="0022" copy_if="index('${iMSCP::Servers::Ftpd}', '::Proftpd::') != -1">${ROOT_CONF}/logrotate.d/proftpd-basic</copy_config>
</install>
