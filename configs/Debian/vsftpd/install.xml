<?xml version="1.0" encoding="UTF-8"?>
<install>
    <folder umask="0022" create_if="index('${iMSCP::Servers::Ftpd}', '::Vsftpd::') != -1">${ROOT_CONF}/logrotate.d</folder>
    <folder umask="0022" create_if="index('${iMSCP::Servers::Ftpd}', '::Vsftpd::') != -1">${ROOT_CONF}/vsftpd</folder>
    <folder umask="0022" create_if="index('${iMSCP::Servers::Ftpd}', '::Vsftpd::') != -1">${ROOT_CONF}/vsftpd/imscp</folder>
    <folder>${SYSTEM_CONF}/vsftpd</folder>
    <copy_config>${SYSTEM_CONF}/vsftpd/vsftpd.conf</copy_config>
    <copy_config>${SYSTEM_CONF}/vsftpd/vsftpd.data.dist</copy_config>
    <copy_config>${SYSTEM_CONF}/vsftpd/vsftpd_pam.conf</copy_config>
    <copy_config>${SYSTEM_CONF}/vsftpd/vsftpd_user.conf</copy_config>
    <copy_config umask="0022" copy_if="index('${iMSCP::Servers::Ftpd}', '::Vsftpd::') != -1">${ROOT_CONF}/logrotate.d/vsftpd</copy_config>
</install>
